; #!/usr/bin/env csi -s 
:above code might be needed to run
; running CHICKEN language, single semi colon is for comments
$ csi
(define myList '(A 1 B 2 C 3 D 4))
(list-ref myList 1)
(list-ref myList 3)
(list-ref myList 4)
(list-ref myList 5)
(list-ref myList 6)
(list-ref myList 7)
(list-ref myList 8)
